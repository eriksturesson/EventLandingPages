rules_version = '2';
service firebase.storage {
  match /b/stockholm-city-affarsnatverk.appspot.com/o {
    match /{allPaths=**} {
        allow read;
        allow write: if request.auth != null;
    }
  }
}

